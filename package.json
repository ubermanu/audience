{
  "name": "audience",
  "description": "Crowd reaction soundboard",
  "homepage": "https://ubermanu.github.io/audience",
  "version": "2.3.0",
  "private": false,
  "license": "MIT",
  "scripts": {
    "dev": "snowpack dev",
    "build": "snowpack build && workbox generateSW",
    "predeploy": "export PUBLIC_URL='/audience/' && yarn build && cp public/.nojekyll build/",
    "deploy": "gh-pages -t -d build"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "svelte": "^3.32.3"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.5.2",
    "gh-pages": "^3.1.0",
    "prettier": "^2.2.1",
    "snowpack": "^3.0.11",
    "workbox-cli": "^6.1.0"
  }
}
